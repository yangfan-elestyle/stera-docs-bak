# Docs site environment.
# Leave unset for local dev and non-prod deploys.
# Set to `product` only when targeting production.
# DOCS_ENV=product

# 首个管理员。仅在账号表为空时生效, 之后改这两个值没有任何作用。
# 线上从 k8s secret 注入; 不设则后台显示「尚未初始化」且没有任何账号可登录。
# ADMIN_EMAIL=ops@example.com
# ADMIN_PASSWORD=
